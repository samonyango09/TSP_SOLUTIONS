# Shared password gating the whole app (sales-team internal tool, not public).
APP_PASSWORD=changeme

# Random string used to sign the session cookie. Generate a real one before deploying:
#   python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=dev-only-change-me
